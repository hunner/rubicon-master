usermanagement::user { 'hunner':
  ensure => present,
  group  => 'users',
}
usermanagement::user { 'elvis':
  ensure => present,
  group  => 'users',
}
usermanagement::user { 'binford':
  ensure => present,
  group  => 'users',
}
